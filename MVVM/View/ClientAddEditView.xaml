<UserControl x:Class="Telecom_ThesisProject.MVVM.View.ClientAddEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <Label Content="Фамилия"/>
            <TextBox Text="{Binding Client.LastName, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Имя"/>
            <TextBox Text="{Binding Client.FirstName, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Отчество"/>
            <TextBox Text="{Binding Client.MiddleName, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Номер договора"/>
            <TextBox Text="{Binding Client.ContractNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Адрес"/>
            <ComboBox ItemsSource="{Binding Addresses}"
                      DisplayMemberPath="DisplayLine"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding Client.AddressId, Mode=TwoWay}"/>

            <Button Content="Сохранить"
                Command="{Binding SaveCommand}" Margin="0,12,0,0"/>

            <Button Content="Назад"
                Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>

</UserControl>
