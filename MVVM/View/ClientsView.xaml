<UserControl x:Class="Telecom_ThesisProject.MVVM.View.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Margin="10" Padding="10" CornerRadius="10"
            Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:TextBlock Text="Клиенты"
                          FontTypography="Subtitle"
                          Padding="5"
                          Grid.Row="0"/>

            <ui:Card Grid.Row="1" >
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="Добавить клиента"
                               Command="{Binding AddCommand}"
                               Icon="{ui:SymbolIcon Add24}"
                               Margin="0,0,5,0"
                               ToolTip="Добавить клиента"/>

                    <Separator Margin="5,2"/>

                    <ui:Button Icon="{ui:SymbolIcon Edit24}"
                               Command="{Binding EditCommand}"
                               Margin="5,0"
                               ToolTip="Редактировать выбранного клиента"/>

                    <ui:Button Icon="{ui:SymbolIcon Delete24}"
                               Command="{Binding DeleteCommand}"
                               Margin="0,0,5,0"
                               ToolTip="Удалить выбранного клиента"/>
                    
                    <Separator Margin="5,2"/>
                    
                    <ui:TextBox Icon="{ui:SymbolIcon Search24}" PlaceholderText="Поиск клиента по имени или IP"
                                Width="300" Margin="5,0"
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

            </ui:Card>

            <ui:Card Grid.Row="2" VerticalAlignment="Top" Margin="0,5,0,0">
                <DataGrid ItemsSource="{Binding Clients}"
                          SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                        <DataGridTextColumn Header="ФИО" Binding="{Binding GetFullName}" Width="*"/>
                        <DataGridTextColumn Header="Договор" Binding="{Binding ContractNumber}" Width="*"/>
                    </DataGrid.Columns>

                    <DataGrid.InputBindings>
                        <MouseBinding Gesture="LeftDoubleClick"
                               Command="{Binding EditCommand}" />
                    </DataGrid.InputBindings>
                </DataGrid>
            </ui:Card>
            

        </Grid>
    </Border>
</UserControl>
