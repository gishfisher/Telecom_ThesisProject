<UserControl x:Class="Telecom_ThesisProject.MVVM.View.BindDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500"
             Background="White">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Привязать оборудование" FontSize="14pt" FontWeight="DemiBold" Margin="0,0,0,15"/>

            <Label Content="Точка монтажа"/>
            <ComboBox ItemsSource="{Binding MountingPoints}"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding SelectedMountingPointId}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding PointType.Name}"/>
                            <TextBlock Text=" — "/>
                            <TextBlock Text="{Binding LocationDescription}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Label Content="Свободное оборудование (без точки монтажа)"/>
            <ComboBox ItemsSource="{Binding FreeDevices}"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding SelectedDeviceId}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}"/>
                            <TextBlock Text=" — "/>
                            <TextBlock Text="{Binding IpAddress}"/>
                            <TextBlock Text=" ("/>
                            <TextBlock Text="{Binding DeviceType.TypeName}"/>
                            <TextBlock Text=")"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>

            <Button Content="Привязать" Command="{Binding BindCommand}" Margin="0,15,0,5"/>
            <Button Content="Назад" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
