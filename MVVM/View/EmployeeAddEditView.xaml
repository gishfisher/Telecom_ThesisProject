<UserControl x:Class="Telecom_ThesisProject.MVVM.View.EmployeeAddEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Employee.FirstName}"/>
            <TextBox Text="{Binding Employee.LastName}"/>
            <TextBox Text="{Binding Employee.MiddleName}"/>

            <TextBox Text="{Binding Employee.User.Login}"/>
            
            <PasswordBox x:Name="PasswordInput"/>

            <ComboBox ItemsSource="{Binding Roles}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding Employee.User.RoleId, Mode=TwoWay}" />

            <CheckBox Content="Активный"
                      IsChecked="{Binding Employee.User.IsActive}"/>

            <Button Content="Сохранить"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding ElementName=PasswordInput}" />

            <Button Content="Назад"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
