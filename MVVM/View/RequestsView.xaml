<UserControl x:Class="Telecom_ThesisProject.MVVM.View.RequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Requests"
               FontSize="14pt"
               FontWeight="DemiBold" 
               Foreground="#111827"
               Padding="5"
               Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Добавить"
                Command="{Binding AddCommand}"/>

            <Button Content="Редактировать"
                Command="{Binding EditCommand}"/>

            <Button Content="Удалить"
                Command="{Binding DeleteCommand}"/>

            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Requests}"
              SelectedItem="{Binding SelectedRequest, Mode=TwoWay}"
              AutoGenerateColumns="False"
              Grid.Row="2" 
              IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                <!--<DataGridTextColumn Header="Наименование" Binding="{Binding Title}" Width="*"/>-->
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type.Name}" Width="*"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.GetFullName}" Width="*"/>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding Employee.GetFullName}" Width="*"/>
            </DataGrid.Columns>

            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
              Command="{Binding EditCommand}" />
            </DataGrid.InputBindings>
        </DataGrid>
    </Grid>
</UserControl>
