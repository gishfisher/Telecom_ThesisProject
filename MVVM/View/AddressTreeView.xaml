<UserControl x:Class="Telecom_ThesisProject.MVVM.View.AddressTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             xmlns:vm="clr-namespace:Telecom_ThesisProject.MVVM.ViewModel" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type vm:CityNodeViewModel}" ItemsSource="{Binding Streets}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2">
                <ui:SymbolIcon Symbol="City24" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type vm:StreetNodeViewModel}" ItemsSource="{Binding Addresses}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2">
                <ui:SymbolIcon Symbol="Location12" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type vm:AddressNodeViewModel}" ItemsSource="{Binding Apartments}">
            <StackPanel Orientation="Horizontal" Background="Transparent" 
                        HorizontalAlignment="Stretch" Margin="0,2">
                <StackPanel.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding DetailsCommand}"/>
                </StackPanel.InputBindings>
                <ui:SymbolIcon Symbol="Home24" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding DisplayText}" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type vm:ApartmentNodeViewModel}">
            <StackPanel Orientation="Horizontal" Background="Transparent" 
                        HorizontalAlignment="Stretch" Margin="0,2">
                <ui:SymbolIcon Symbol="Door20" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding DisplayText}" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Border Margin="10" Padding="10" CornerRadius="10" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:TextBlock Text="Журнал адресов"
                          FontTypography="Subtitle"
                          Padding="5"
                          Grid.Row="0"/>

            <ui:Card Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <ui:Button Icon="{ui:SymbolIcon Add24}" Content="Город" Command="{Binding AddCityCommand}" Margin="5,0"/>
                    <ui:Button Icon="{ui:SymbolIcon Location12}" Content="Улица" Command="{Binding AddStreetCommand}" Margin="0,0,5,0"/>
                    <ui:Button Icon="{ui:SymbolIcon HomeAdd24}" Content="Дом" Command="{Binding AddAddressCommand}" Margin="0,0,5,0"/>
                    <ui:Button Icon="{ui:SymbolIcon Door20}" Content="Квартира" Command="{Binding AddApartmentCommand}" Margin="0,0,5,0"/>

                    <Separator Margin="5,2"/>

                    <ui:Button Icon="{ui:SymbolIcon Edit24}" Command="{Binding EditCommand}" Margin="5,0"/>
                    <ui:Button Icon="{ui:SymbolIcon Delete24}" Command="{Binding DeleteCommand}" Margin="0,0,5,0"/>
                    <ui:Button Icon="{ui:SymbolIcon ArrowClockwise24}" Command="{Binding RefreshCommand}" Margin="0,0,5,0"/>
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Row="2" Padding="0" VerticalAlignment="Top" Margin="0,5,0,0">
                <TreeView ItemsSource="{Binding Cities}"
                          BorderThickness="0"
                          Background="Transparent"
                          SelectedItemChanged="TreeView_SelectedItemChanged">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                            <Setter Property="Padding" Value="8,5"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </ui:Card>
        </Grid>
    </Border>
</UserControl>
