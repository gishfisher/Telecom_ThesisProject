<UserControl x:Class="Telecom_ThesisProject.MVVM.View.NetworkDevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
    <Border Padding="10" Margin="10" CornerRadius="10"
            Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Сетевые устройства"
               FontSize="14pt"
               Foreground="#111827"
               Padding="5"
               Grid.Row="0"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Content="Добавить" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
                <Button Content="Редактировать" Command="{Binding EditCommand}" Margin="0,0,8,0"/>
                <Button Content="Удалить" Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
                <TextBox Width="220" VerticalContentAlignment="Center"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Поиск по имени или IP"/>
            </StackPanel>

            <ui:DataGrid Grid.Row="2"
              ItemsSource="{Binding Devices}"
              SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
              AutoGenerateColumns="False"
              IsReadOnly="True">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="*"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding DeviceType.TypeName}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Мониторинг" Binding="{Binding IsMonitored}" Width="*"/>
                    <DataGridTextColumn Header="Родитель" Binding="{Binding ParentDevice.Name}" Width="*"/>
                </ui:DataGrid.Columns>
                <ui:DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenDeviceDetailsCommand}"/>
                </ui:DataGrid.InputBindings>
            </ui:DataGrid>
        </Grid>
    </Border>
</UserControl>
