<UserControl x:Class="Telecom_ThesisProject.MVVM.View.TariffAddEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="900"
             Background="Transparent">

    <Border Padding="10"
            CornerRadius="10"
            Background="White">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="0,0,0,10">

                <ui:Button Icon="{ui:SymbolIcon ArrowLeft20}"
                           Command="{Binding CancelCommand}"
                           Padding="10"
                           Margin="0,0,5,0"
                           ToolTip="Назад"/>

                <ui:Button Icon="{ui:SymbolIcon Save20}"
                           Command="{Binding SaveCommand}"
                           Padding="10"
                           ToolTip="Сохранить"
                           Appearance="Primary"/>

                <Separator Margin="10,2"/>

                <ui:TextBlock Text="{Binding Title, FallbackValue=Тариф}"
                              VerticalAlignment="Center"
                              FontTypography="Subtitle"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>

                    <ui:CardExpander IsExpanded="True"
                                     Padding="10">

                        <ui:CardExpander.Header>
                            <ui:TextBlock FontTypography="BodyStrong">
                                Основная информация
                            </ui:TextBlock>
                        </ui:CardExpander.Header>

                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock Text="Наименование тарифа"
                                          VerticalAlignment="Center"/>
                            <ui:TextBox Grid.Column="1"
                                        Text="{Binding Tariff.Name, UpdateSourceTrigger=PropertyChanged}"
                                        PlaceholderText="Введите название тарифа"/>

                            <ui:TextBlock Grid.Row="1"
                                          Text="Абонентская плата"
                                          Margin="0,12,0,0"
                                          VerticalAlignment="Center"/>
                            <ui:TextBox Grid.Row="1" Grid.Column="1"
                                        Margin="0,12,0,0"
                                        Text="{Binding Tariff.MonthlyFee,
                                                       UpdateSourceTrigger=PropertyChanged,
                                                       StringFormat=F2}"
                                        PlaceholderText="0.00"/>
                        </Grid>
                    </ui:CardExpander>

                    <ui:CardExpander Margin="0,10,0,0"
                                     IsExpanded="True"
                                     Padding="10">

                        <ui:CardExpander.Header>
                            <StackPanel>
                                <ui:TextBlock FontTypography="BodyStrong">
                                    Состав тарифа
                                </ui:TextBlock>
                            </StackPanel>
                        </ui:CardExpander.Header>

                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Content="Услуги"/>

                                <ComboBox Grid.Column="0" Grid.Row="1"
                                          Height="35"
                                          ItemsSource="{Binding AvailableServices}"
                                          SelectedItem="{Binding SelectedServiceToAdd}"
                                          DisplayMemberPath="Name"/>

                                <ui:Button Grid.Column="1" Grid.Row="1"
                                           Margin="10,0,0,0"
                                           Padding="10,5"
                                           Icon="{ui:SymbolIcon Add24}"
                                           Content="Добавить"
                                           Command="{Binding AddServiceCommand}"/>
                            </Grid>

                            <ui:DataGrid Margin="0,12,0,0"
                                         ItemsSource="{Binding SelectedServices}"
                                         AutoGenerateColumns="False"
                                         IsReadOnly="True"
                                         MinHeight="140"
                                         HeadersVisibility="Column"
                                         BorderThickness="0">

                                <ui:DataGrid.Columns>
                                    <DataGridTextColumn Header="Наименование"
                                                        Width="*"
                                                        Binding="{Binding Name}">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Margin" Value="10,0"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ui:Button Content="Удалить"
                                                           Padding="6,2"
                                                           Foreground="#EF4444"
                                                           Command="{Binding DataContext.RemoveServiceCommand,
                                                                             RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                           CommandParameter="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </ui:DataGrid.Columns>
                            </ui:DataGrid>
                        </StackPanel>
                    </ui:CardExpander>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>