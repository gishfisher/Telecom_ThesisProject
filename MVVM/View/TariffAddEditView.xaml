<UserControl x:Class="Telecom_ThesisProject.MVVM.View.TariffAddEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <Label Content="Наименование"/>
            <TextBox Text="{Binding Tariff.Name, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Абонентская плата (в месяц)"/>
            <TextBox Text="{Binding Tariff.MonthlyFee, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>

            <Label Content="Цена за 1 ГБ"/>
            <TextBox Text="{Binding Tariff.PricePerGb, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>

            <DockPanel Margin="0,8,0,0">
                <Button Content="➕ Добавить услугу"
                        DockPanel.Dock="Right"
                        Margin="8,0,0,0"
                        Foreground="#2C3E50"
                        Command="{Binding AddServiceCommand}"/>
                <ComboBox Height="30"
                          ItemsSource="{Binding AvailableServices}"
                          SelectedItem="{Binding SelectedServiceToAdd}"
                          DisplayMemberPath="Name"
                          Tag="Выберите услугу"/>
            </DockPanel>

            <DataGrid ItemsSource="{Binding SelectedServices}"
                      IsReadOnly="True" AutoGenerateColumns="False"
                      MinHeight="120"
                      Margin="0,8,0,0"
                      HorizontalScrollBarVisibility="Disabled" 
                      VerticalScrollBarVisibility="Auto">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Наименование" Width="*" Binding="{Binding Name}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="10,0"/>
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="❌" 
                                        FontSize="14"
                                        Foreground="#EF4444"
                                        Command="{Binding DataContext.RemoveServiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

            <Button Content="Сохранить"
                Command="{Binding SaveCommand}" Margin="0,12,0,0"/>

            <Button Content="Назад"
                Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
