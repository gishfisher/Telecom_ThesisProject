<UserControl x:Class="Telecom_ThesisProject.MVVM.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Telecom_ThesisProject.MVVM.View"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border MaxWidth="400" Height="325"
            VerticalAlignment="Center" HorizontalAlignment="Center"
            Background="#FFFFFF" Width="350"
            CornerRadius="10">
        
        <Grid Margin="25">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:SymbolIcon Symbol="ShieldKeyhole24" 
                           Margin="20,10,20,10" FontSize="24pt"
                           Foreground="{DynamicResource AccentFillColorDefaultBrush}"/>

            <ui:TextBlock Text="Авторизация" Grid.Row="1" 
                          HorizontalAlignment="Center"
                          FontTypography="Subtitle" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="2">
                <Label Content="Логин"/>
                <ui:TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="Ведите логин"/>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <Label Content="Пароль"/>
                <ui:PasswordBox x:Name="PBPassword" 
                                PlaceholderText="Введите пароль" 
                                Icon="Password24"/>
            </StackPanel>

            <ui:Button Content="Войти" Grid.Row="4"
                       Command="{Binding LoginCommand}" 
                       CommandParameter="{Binding ElementName=PBPassword}"
                       Height="35" Appearance="Primary" 
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Bottom"/>
        </Grid>
    </Border>
</UserControl>
